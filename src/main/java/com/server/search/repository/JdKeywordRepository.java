package com.server.search.repository;

import com.server.search.domain.JdKeyword;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JdKeywordRepository extends JpaRepository<JdKeyword, Long> {
}
